/*
-title: Structure Padding.
-When an object of some structure type is declared then some contiguous block of memory will be allocatged to structure members.
*/
#include <stdio.h>
struct abc
  {
    char a;
    char b;
    int c;
  } var;

int main(void)
{
  
  return (0);
}
